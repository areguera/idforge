== Environment

IDFORGE_LIBRARY::
    Specifies the directory where *idforge(1)* common functions and
    modules are stored in.  When this variable isn't defined in the
    command-line, it is set to +/usr/libexec/idforge+ by default.
